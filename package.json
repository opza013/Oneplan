{
  "name": "oneplan-range-operation-platform",
  "version": "4.1.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "description": "OnePlan Range Operation Platform complete Render rebuild with shared cloud state, responsive team workload stack, Detail Plan, Gantt, filters, and audit controls.",
  "scripts": {
    "start": "node server.js",
    "validate": "node --check server.js && node test/validate-inline.mjs",
    "test": "npm run validate && node test/smoke.mjs"
  },
  "engines": {
    "node": ">=20 <25"
  }
}
