{
  "name": "oneplan-range-operation-platform",
  "version": "4.0.2",
  "private": true,
  "type": "module",
  "main": "server.js",
  "description": "OnePlan Range Operation Platform with AI Radar-style local cache and server JSON state synchronization.",
  "scripts": {
    "start": "node server.js",
    "validate": "node --check server.js && node test/validate-inline.mjs",
    "test": "npm run validate && node test/smoke.mjs"
  },
  "engines": {
    "node": ">=20 <25"
  }
}
