# ONE PLAN v5.4.8 — Master Data Layout Alignment

- Task Master and NWT Master are locked to the same row height and top/bottom alignment.
- Key Milestone Master and Capacity Model are locked to the same row height and top/bottom alignment.
- Tables scroll inside the card instead of expanding one card and shifting the paired card.
- Generic `.card + .card` spacing is neutralized inside the Master Data grid to prevent vertical offsets.
- Responsive layout returns to single-column cards on narrower screens.
