This directory is included for local development only.

On Render, the persistent disk is mounted at `/var/data`, and the state files are stored there according to the environment variables in `render.yaml`.
