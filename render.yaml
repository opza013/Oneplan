services:
  - type: web
    name: oneplan-cloud-starter
    runtime: static
    buildCommand: echo "OnePlan Detail Gantt milestone panel build ready"
    staticPublishPath: .
    autoDeployTrigger: commit
